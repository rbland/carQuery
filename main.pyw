#!/usr/bin/env python

# ---------------------------------------------------------------------------- #
# -- WINDOW ENTRY POINT ------------------------------------------------------ #

# a Windows wrapper to supress the the command prompt.
import start
start.safe_exit(start.main())
